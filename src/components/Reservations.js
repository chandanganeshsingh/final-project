import '../App.css';
import Footer from './Footer';
import Header from './Header';

const Reservations = () => {
  return (
    <div>
      <Header />
      <Footer />
    </div>
  );
};

export default Reservations;